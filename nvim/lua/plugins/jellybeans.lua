-- =============================================================================
-- jellybeans.lua  (Alternative colorscheme — matches MacVim setup)
-- =============================================================================
-- Catppuccin stays the default (set in lua/config/colorscheme.lua).
-- To switch on demand:
--   :colorscheme jellybeans
-- To make it the default, edit lua/config/colorscheme.lua.
-- =============================================================================

-- Make jellybeans render full 256-color palette
vim.g.jellybeans_use_term_italics = 1
